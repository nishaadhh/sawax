
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    <title>SAWAX | User Profile</title>
    <style>
        :root {
            --primary: #1c2526;
            --secondary: #f5f6f5;
            --accent: #d4a017;
            --text: #2d3436;
            --light-text: #636e72;
            --border: #dfe6e9;
            --gradient: linear-gradient(135deg, #d4a017, #e7c156);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Inter', 'Helvetica Neue', Arial, sans-serif;
        }

        body {
            color: var(--text);
            overflow-x: hidden;
            background: linear-gradient(to bottom, var(--secondary), #e8ecef);
        }

        a {
            text-decoration: none;
            color: inherit;
        }

        .container {
            width: 100%;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Header */
        header {
            background: var(--primary);
            color: white;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            border-bottom: 1px solid var(--accent);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 25px 0;
        }

       

      
        nav ul {
            display: flex;
            list-style: none;
            gap: 40px;
        }

        nav ul li a {
            font-weight: 600;
            font-size: 17px;
            color: white;
            transition: color 0.3s ease, transform 0.2s ease;
        }

        nav ul li a:hover {
            color: var(--accent);
            transform: translateY(-2px);
        }

        .header-icons {
            display: flex;
            gap: 25px;
            align-items: center;
        }

        .header-icon {
            cursor: pointer;
            font-size: 22px;
            color: white;
            transition: color 0.3s ease, transform 0.2s ease;
        }

        .header-icon:hover {
            color: var(--accent);
            transform: scale(1.1);
        }

        .mobile-menu {
            display: none;
            font-size: 26px;
            cursor: pointer;
        }

        .dropdown {
            position: relative;
            display: inline-block;
        }

        .sign-in-link {
            color: var(--accent);
            text-decoration: none;
            padding: 1px;
            display: block;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background: var(--primary);
            min-width: 180px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
            z-index: 1000;
            right: 0;
            border-radius: 8px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .dropdown-content a {
            color: white;
            padding: 14px 18px;
            text-decoration: none;
            display: block;
            font-size: 15px;
        }

        .dropdown-content a:hover {
            background: var(--accent);
            color: var(--primary);
            border-radius: 8px;
        }

        .dropdown:hover .dropdown-content {
            display: block;
        }

        /* Profile Section */
        .profile {
            padding: 100px 0;
            background: var(--secondary);
        }

        .section-title {
            text-align: center;
            margin-bottom: 50px;
        }

        .section-title h2 {
            font-size: 42px;
            font-weight: 700;
            margin-bottom: 15px;
            color: var(--primary);
            letter-spacing: 1px;
        }

        .section-title h2::after {
            content: '';
            position: absolute;
            bottom: -2px;
            left: 50%;
            transform: translateX(-50%);
            width: 80px;
            height: 3px;
            background: var(--gradient);
        }

        .section-title p {
            color: var(--light-text);
            font-size: 20px;
            max-width: 700px;
            margin: 0 auto;
            font-weight: 300;
        }

        .profile-content {
            display: grid;
            grid-template-columns: 1fr 3fr;
            gap: 40px;
        }

        .profile-sidebar {
            background: white;
            border-radius: 12px;
            padding: 30px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
            border: 1px solid var(--border);
        }

        .profile-sidebar h3 {
            font-size: 22px;
            font-weight: 700;
            margin-bottom: 25px;
            color: var(--primary);
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .profile-sidebar ul {
            list-style: none;
        }

        .profile-sidebar ul li {
            margin-bottom: 15px;
        }

        .profile-sidebar ul li a {
            font-size: 17px;
            color: var(--text);
            transition: color 0.3s ease, padding-left 0.3s ease;
        }

        .profile-sidebar ul li a:hover {
            color: var(--accent);
            padding-left: 10px;
        }

        .profile-main {
            background: white;
            border-radius: 12px;
            padding: 40px;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.08);
            border: 1px solid var(--border);
        }

        .profile-image-section {
            display: flex;
            align-items: center;
            gap: 25px;
            margin margin-bottom: 40px;
        }

        .profile-image {
            width: 120px;
            height: 120px;
            border-radius: 50%;
            overflow: hidden;
            background: black;
            padding: 4px;
            transition: transform 0.3s ease;
        }

        .profile-image:hover {
            transform: scale(1.05);
        }

        .profile-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 50%;
            border: 2px solid white;
        }

        .profile-image-upload {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .profile-image-upload label {
            cursor: pointer;
            padding: 12px 25px;
            background: var(--gradient);
            color: white;
            border-radius: 6px;
            font-size: 15px;
            font-weight: 700;
            text-transform: uppercase;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .profile-image-upload label:hover {
            transform: translateY(-3px);
            box-shadow: 0 4px 15px rgba(212, 160, 23, 0.3);
        }

        .profile-image-upload input[type="file"] {
            display: none;
        }

        .profile-form h3 {
            font-size: 26px;
            font-weight: 700;
            margin-bottom: 25px;
            color: var(--primary);
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .form-group {
            margin-bottom: 20px;
        }

        .form-group label {
            display: block;
            font-size: 15px;
            font-weight: 600;
            margin-bottom: 10px;
            color: var(--text);
        }

        .form-group input {
            width: 100%;
            padding: 14px;
            border: 1px solid var(--border);
            border-radius: 6px;
            font-size: 16px;
            background: #fafafa;
            transition: border-color 0.3s ease, box-shadow 0.3s ease;
        }

        .form-group input:focus {
            outline: none;
            border-color: var(--accent);
            box-shadow: 0 0 8px rgba(212, 160, 23, 0.2);
        }

        .btn {
            display: inline-block;
            padding: 14px 35px;
            background: var(--gradient);
            color: white;
            font-weight: 700;
            border-radius: 6px;
            border: none;
            cursor: pointer;
            text-transform: uppercase;
            letter-spacing: 1.5px;
            font-size: 15px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 4px 15px rgba(212, 160, 23, 0.3);
        }

        .order-history {
            margin-top: 40px;
        }

        .order-history h3 {
            font-size: 26px;
            font-weight: 700;
            margin-bottom: 25px;
            color: var(--primary);
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .order-table {
            width: 100%;
            border-collapse: collapse;
        }

        .order-table th,
        .order-table td {
            padding: 18px;
            text-align: left;
            border-bottom: 1px solid var(--border);
        }

        .order-table th {
            font-weight: 700;
            color: var(--primary);
            background: #fafafa;
        }

        .order-table td {
            color: var(--light-text);
        }

        .order-table a {
            color: var(--accent);
            font-weight: 600;
            transition: color 0.3s;
        }

        .order-table a:hover {
            color: #b38e12;
        }

        /* Footer */
        footer {
            background: var(--primary);
            color: white;
            padding: 80px 0 40px;
            border-top: 3px solid var(--accent);
        }

        .footer-content {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 40px;
            margin-bottom: 40px;
        }

        .footer-logo {
            font-size: 32px;
            font-weight: 800;
            letter-spacing: 3px;
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .footer-logo span {
            color: var(--accent);
        }

        .footer-about p {
            opacity: 0.8;
            line-height: 1.7;
            margin-bottom: 20px;
            font-size: 15px;
        }

        .social-icons {
            display: flex;
            gap: 15px;
        }

        .social-icon {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.15);
            display: flex;
            align-items: center;
            justify-content: center;
            transition: background 0.3s ease, transform 0.3s ease;
        }

        .social-icon:hover {
            background: var(--gradient);
            transform: scale(1.1);
        }

        /* .footer-links h4 {
            font-size: 20px;
            font-weight: 700;
            margin-bottom: 25px;
            background: var(--gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .footer-links h4::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 50px;
            height: 3px;
            background: var(--gradient);
        } */

        .footer-links ul {
            list-style: none;
        }

        .footer-links ul li {
            margin-bottom: 10px;
        }

        .footer-links ul li a {
            opacity: 0.8;
            font-size: 15px;
            transition: opacity 0.3s, color 0.3s;
        }

        .footer-links ul li a:hover {
            opacity: 1;
            color: var(--accent);
        }

        .contact-item {
            display: flex;
            gap: 15px;
            margin-bottom: 15px;
            opacity: 0.8;
            font-size: 15px;
        }

        .contact-icon {
            font-size: 18px;
        }

        .footer-bottom {
            border-top: 1px solid rgba(255, 255, 255, 0.15);
            padding-top: 25px;
            display: flex;
            justify-content: space-between;
            opacity: 0.7;
            font-size: 15px;
        }

        /* Responsive Design */
        @media (max-width: 992px) {
            .profile-content {
                grid-template-columns: 1fr;
            }

            .profile-image {
                width: 120px;
                height: 120px;
            }

            .footer-content {
                grid-template-columns: 1fr 1fr;
            }
        }

        @media (max-width: 768px) {
            nav ul {
                display: none;
            }

            .mobile-menu {
                display: block;
            }

            .profile {
                padding: 80px 0;
            }

            .profile-main {
                padding: 25px;
            }

            .profile-image-section {
                flex-direction: column;
                align-items: flex-start;
            }

            .order-table {
                display: block;
                overflow-x: auto;
                white-space: nowrap;
            }

            .order-table th,
            .order-table td {
                padding: 12px;
                font-size: 15px;
            }

            .footer-content {
                grid-template-columns: 1fr;
                gap: 40px;
            }

            .footer-bottom {
                flex-direction: column;
                gap: 15px;
                text-align: center;
            }
        }

        @media (max-width: 576px) {
            .section-title h2 {
                font-size: 32px;
            }

            .section-title p {
                font-size: 18px;
            }

            .profile-image {
                width: 100px;
                height: 100px;
            }

            .form-group input {
                font-size: 15px;
            }

            .btn {
                font-size: 13px;
                padding: 12px 25px;
            }

            .profile-form h3,
            .order-history h3 {
                font-size: 22px;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <%- include("../../views/partials/user/header") %>
    <!-- Profile Section -->
    <section class="profile">
        <div class="container">
            <div class="section-title">
                <h2>Your Profile</h2>
                <p>Manage your personal details, update your profile picture, and view your order history.</p>
            </div>
            <div class="profile-content">
                <div class="profile-sidebar">
                    <h3>Account Menu</h3>
                    <ul>
                        <li><a href="profile">Edit Profile</a></li>
                        <li><a href="/orders">Order History</a></li>
                        <li><a href="wishlist">Wishlist</a></li>
                        <li><a href="/address">Add Address</a></li>
                        <li><a href="/wallet">Wallet</a></li>
                        <li><a href="/coupon">Coupons</a></li>
                        <li><a href="/logout" style="color: red;">Logout <i class="fa-solid fa-right-from-bracket"></i></a></li>
                    </ul>
                </div>
                <div class="profile-main">
                    <div class="profile-image-section" id="profile">
                        <div class="profile-image">
                            <!-- <img src="<%= user.profileImage || '/api/placeholder/120/120' %>" id="profile-img" alt="Profile Image"> -->
                        </div>
                        <div class="profile-image-upload">
                            <h5 style="font-weight: 900;"><%= user.name %></h5>
                            <!-- <label for="image-upload">Change Profile Picture</label> -->
                            <input type="file" id="image-upload" accept="image/*">
                        </div>
                    </div>
                    <div class="profile-form">
                        <h3>Edit Profile</h3>
                        <form action="/userProfile" method="POST">
                            <div class="form-group">
                                <label for="name">Full Name</label>
                                <input type="text" id="name" name="name" value="<%= user.name %>" required>
                            </div>
                            <div class="form-group">
                                <label for="email">Email Address</label>
                                <input type="email" id="email" name="email" value="<%= user.email %>" required>
                            </div>
                            <a href="/change-email" class="btn">Change Email</a>
                            <a href="/change-password" class="btn">Change Password</a>
                        </form>
                    </div>
                    <!-- <div class="order-history" id="orders">
                        <h3>Order History</h3>
                        <table class="order-table">
                            <thead>
                                <tr>
                                    <th>Order ID</th>
                                    <th>Date</th>
                                    <th>Total</th>
                                    <th>Status</th>
                                    <th>Action</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>#12345</td>
                                    <td>2025-04-20</td>
                                    <td>$299.00</td>
                                    <td>Delivered</td>
                                    <td><a href="#">View Details</a></td>
                                </tr>
                                <tr>
                                    <td>#12346</td>
                                    <td>2025-03-15</td>
                                    <td>$189.00</td>
                                    <td>Shipped</td>
                                    <td><a href="#">View Details</a></td>
                                </tr>
                            </tbody>
                        </table>
                    </div> -->
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <%- include("../../views/partials/user/footer") %>

    <!-- JavaScript for Image Preview -->
    <script>
        async function uploadProfileImage(file) {
            try {
                const formData = new FormData();
                formData.append('profileImage', file);

                const response = await fetch('/upload-profile-image', {
                    method: 'POST',
                    body: formData
                });

                const data = await response.json();
                if (data.success) {
                    alert('Profile picture updated successfully!');
                    window.location.reload();
                } else {
                    alert('Error uploading profile picture: ' + (data.message || 'Unknown error'));
                }
            } catch (error) {
                console.error('Error uploading profile picture:', error);
                alert('Error uploading profile picture. Please try again.');
            }
        }
    </script>
